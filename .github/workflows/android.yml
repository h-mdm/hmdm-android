name: Android CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    # Run lint and unit tests first to catch errors quickly
    - name: Run Unit Tests
      run: ./gradlew testDebugUnitTest

    # Build the debug APK specifically to avoid signing issues
    - name: Build Debug APK
      run: ./gradlew assembleDebug

    # Upload the APK so you can actually download it from GitHub
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: debug-apk
        path: app/build/outputs/apk/debug/app-debug.apk
